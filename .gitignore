.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
coverage.xml
htmlcov/
auth/storage_state.json
*.tmp

# Personal data and config — never commit (see launch plan §7)
data/*
!data/.gitkeep
config.toml

# Working docs (not tracked)
zz-support-files/
# Plan artifacts: briefs, reviews and full `*-full.diff` dumps of the branch under work.
# Untracked but unignored, a single `git add -A` would have committed the whole plan.
.superpowers/

# Local secrets (TYPESAFE_API_KEY); see .env.example
.env
